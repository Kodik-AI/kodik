{
  "servers": {
    "stripe": {
      "type": "streamableHttp",
      "url": "https://mcp.stripe.com/",
      "headers": {
        "Authorization": "Bearer ${user_config.api_key}"
      }
    }
  },
  "meta": {
    "stripe": {
      "id": "stripe-mcp",
      "title": "stripe",
      "description": "Stripe's official remote MCP. Authenticate with a Stripe API key."
    }
  }
}
