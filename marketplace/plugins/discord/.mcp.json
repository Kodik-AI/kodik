{
  "mcpServers": {
    "discord": {
      "command": "bun",
      "args": ["run", "--cwd", "${KODIK_PLUGIN_ROOT}", "--shell=bun", "--silent", "start"],
      "env": {
        "DISCORD_BOT_TOKEN": "${user_config.bot_token}",
        "DISCORD_ACCESS_MODE": "${user_config.access_mode}",
        "DISCORD_STATE_DIR": "${user_config.state_dir}"
      }
    }
  }
}
